# Screenshots

Drop your captures here with these exact names so the main README renders them:

- `dashboard.png` — the dashboard (hero net worth + insights)
- `reports.png` — the Reports page (net worth trend / income vs expenses)
- `ai-chat.png` — the AI assistant panel (bonus: showing a confirmed action)
- `budget.png` — the Budget planner

You already have most of these from earlier captures — just rename and save them
here. Recommended width: ~1400px, PNG.
